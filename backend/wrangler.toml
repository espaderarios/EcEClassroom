name = "ec-eclassroom-backend"
main = "src/index.js"
compatibility_date = "2026-01-21"

# Optional: set your Cloudflare account_id and environment when ready
# account_id = "YOUR_ACCOUNT_ID"
# workers_dev = true

# To enable the OpenAI-powered AI endpoint, add your secret with:
#
#   cd backend
#   wrangler secret put OPENAI_API_KEY
#
# (Avoid placing API keys directly in this file; use `wrangler secret`.)
